[project]
name = "hse-backend"
version = "0.1.0"
description = "FastAPI backend for the HSE Safety, Compliance & Intelligence Platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
